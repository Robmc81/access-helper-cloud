
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "1998:80"
    environment:
      - NODE_ENV=production
      - NPM_CONFIG_LEGACY_PEER_DEPS=true
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
